<Window x:Class="WpfAppUI.View.TopUpBalanceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAppUI.View"
        mc:Ignorable="d"
        Title="Пополнение баланса" Height="180" Width="350"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Текущий баланс -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Текущий баланс:" 
                   VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBlock Grid.Row="0" Grid.Column="1" x:Name="tbCurrentBalance" 
                   VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Сумма пополнения -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Сумма пополнения:" 
                   VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="tbAmount" 
                 VerticalAlignment="Center" Margin="0,0,0,10"
                 PreviewTextInput="TbAmount_PreviewTextInput"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" 
                    Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="btnTopUp" Content="Пополнить" Width="100" Height="25" 
                    Margin="0,0,10,0" Click="BtnTopUp_Click" IsDefault="True"/>
            <Button x:Name="btnCancel" Content="Отмена" Width="100" Height="25" 
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>