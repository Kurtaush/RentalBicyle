<Window x:Class="WpfAppUI.ViewAdmin.RoutePopularityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAppUI.ViewAdmin"
        mc:Ignorable="d"
        Title="Популярность маршрутов" Height="399" Width="550">
    <StackPanel Background="#FFD8D8D8">
        <Label Style="{StaticResource crudLabel}">
            Самые популярные маршруты
        </Label>
        <ListView ItemsSource="{Binding PopularRoutes}" Height="300" Background="#FFD3CDCD">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Станция начала" DisplayMemberBinding="{Binding StartStation}" Width="200"/>
                    <GridViewColumn Header="Станция окончания" DisplayMemberBinding="{Binding EndStation}" Width="200"/>
                    <GridViewColumn Header="Число поездок" DisplayMemberBinding="{Binding TripCount}" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>
    </StackPanel>
</Window>
